prefix=@prefix@
exec_prefix=@exec_prefix@
libdir=@libdir@
includedir=@includedir@

Name: @PACKAGE_NAME@
Description: Library to access to a ZFS filesystem without fuse.
Requires:
Version: @PACKAGE_VERSION@
Cflags: -I${includedir}
Libs: -L${libdir} -lzfswrap -lcrypto -lz -laio
